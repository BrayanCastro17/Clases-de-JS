// Vamos a definir el modo estricto
'use strict';

// Trabajamos con los métodos
// Existen funciones y existen métodos, en los dos casos son llamados para ejecutar alguna acción
// Los procedimientos -> es un conjunto de instrucciones que cumplen una tarea
// Función -> Se parecen mucho a los procedimientos, pero estas ultimas pueden retornar un valor
// Clases -> Es un concepto de la POO, se usa para encapsular funcionalidades, puede contener campos y métodos
// Los métodos -> Son funciones que pertenecen a un objeto, y se pueden llamar, podrían ser un procedimiento o una función, la única diferencia es que pertenecen a una clase 

// Variables

var intNumero = 0;

// Eventos

// Cuerpo del Sistema

console.log(intNumero);

// Vamos a convertir un dato numérico en una cadena de caracteres, podríamos usar la función STRING(), pero estamos usando el método, .TOSTRING()

console.log(intNumero.toString);

console.log(typeof intNumero);
console.log(typeof intNumero.toString);

// Funciones

// Investigación

/* 
Métodos
-> .toUpperCase()
-> .toLowerCase()
-> .length -> Tanto para texto como para vectores
-> .concat()
-> .indexOf()
-> .lastIndexOf()
-> .search()
-> .match(). guide
-> .substr()
-> .charAt()
-> .starstWith()
-> .replace()
-> .slice()
-> .split()
-> .trim()
-> .include()
*/